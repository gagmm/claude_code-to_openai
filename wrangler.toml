name = "claudecode-toopenai"
main = "index.js"
compatibility_date = "2025-06-27"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = ["*/15 * * * *"]

# KV 绑定通过 Cloudflare Dashboard 配置，不写在这里
# 环境变量通过 Cloudflare Dashboard 配置，不写在这里
